# Dependencies for the final 3DRR Track 2 source package.
# Versions below match the environment used for the final submission.

numpy>=2.0
Pillow>=10.0
scikit-image>=0.24
PyYAML>=6.0
opencv-python>=4.10
tqdm
torch>=2.5
torchvision>=0.20
gsplat>=1.5
